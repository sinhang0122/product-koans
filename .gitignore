# Mac OS
.DS_Store

# Local tooling state
.claude/

# Node / Firebase Admin SDK
node_modules/
package-lock.json

# Firebase CLI deploy cache (매 deploy마다 변경 — 추적 불필요)
.firebase/
firebase-debug.log
firebase-debug.*.log

# 🔐 Firebase 서비스 계정 키 (절대 커밋 금지)
service-account-key.json
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
